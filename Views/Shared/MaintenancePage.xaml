<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="BeauOuPas.Views.Shared.MaintenancePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:tr="clr-namespace:BeauOuPas.Extensions"
    BackgroundColor="{StaticResource DarkBackground}"
    Shell.NavBarIsVisible="False">

    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center"
                         Spacing="24" Padding="40">

        <Label Text="🔧" FontSize="72" HorizontalOptions="Center"/>

        <Label Text="BeauOuPas"
               FontSize="28" FontAttributes="Bold"
               TextColor="{StaticResource Primary}" HorizontalOptions="Center"/>

        <!-- Texte du message (sans l'URL) -->
        <Label x:Name="MessageLabel"
               FontSize="16" TextColor="{StaticResource DarkTextSecondary}"
               HorizontalOptions="Center"
               HorizontalTextAlignment="Center"/>

        <!-- Lien cliquable (visible uniquement si une URL est détectée) -->
        <Label x:Name="LinkLabel"
               FontSize="15" TextColor="{StaticResource Primary}"
               TextDecorations="Underline"
               HorizontalOptions="Center"
               HorizontalTextAlignment="Center"
               IsVisible="False">
            <Label.GestureRecognizers>
                <TapGestureRecognizer x:Name="LinkTap"/>
            </Label.GestureRecognizers>
        </Label>

        <ActivityIndicator IsRunning="True" Color="{StaticResource Primary}"
                           HorizontalOptions="Center" Margin="0,16,0,0"/>

        <Label Text="{tr:Translate Key=Maintenance_Retry}"
               FontSize="12" TextColor="{StaticResource DarkBorder}"
               HorizontalOptions="Center" HorizontalTextAlignment="Center"/>
    </VerticalStackLayout>
</ContentPage>
